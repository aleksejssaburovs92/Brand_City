var validation = {
  validate : function(formID) {
    var form = $(formID);
    var elements = form.elements;
  },
  lang : {
    en : {
      required : 'required',
      email : 'email required'
    },

  }
}
